from __future__ import annotations


def read_active_thread(page) -> dict:
    """Phase 5 stub — read Gmail thread from the active tab."""
    raise NotImplementedError("Gmail browser reader is planned for Phase 5.")
